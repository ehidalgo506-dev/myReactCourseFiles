import React from 'react';

const SeasonDisplay = (props) => {
  console.log(
    '🚀 ~ file: SeasonDisplay.js ~ line 4 ~ SeasonDisplay ~ props',
    props
  );
  console.log(props.lat);
  return <div>Season Display</div>;
};

export default SeasonDisplay;
